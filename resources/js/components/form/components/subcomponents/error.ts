export { default as Error } from '@/components/InputError.vue'

/**
 * We may add more input components in import, and add new types for inputComponent
 */
export interface ErrorComponentType {
    errorComponent: 'Error'
}
