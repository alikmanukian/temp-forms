export { Input } from '@/components/ui/input'

/**
 * We may add more input components in import, and add new types for inputComponent
 */
export interface InputComponentType {
    inputComponent: 'Input'
}
