export { Label } from '@/components/ui/label'

/**
 * We may add more label components in import, and add new types for labelComponent
 */
export interface LabelComponentType {
    labelComponent: 'Label'
}
